# Railway Environment Variables Template
# Copy these to your Railway project environment variables in the dashboard

# Google Service Account Credentials (paste the entire JSON content as a string)
GOOGLE_APPLICATION_CREDENTIALS_JSON=<paste-your-service-account-json-content-here>

# Gemini API Key
GEMINI_API_KEY=<your-gemini-api-key-here>

# Application Settings
DEBUG=false
GEMINI_TIMEOUT=60
MAX_FILE_SIZE_MB=100
MAX_ROWS_FOR_SYNC=10000

# Railway will automatically set PORT, but you can override if needed
# PORT=8000